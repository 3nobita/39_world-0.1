<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
</head>
<body>
    <h1>Search Results</h1>
    <a href="/notes">back</a>
    <ul>
        <% notes.forEach(note => { %>
            <li>
                <strong>Heading:</strong> <%= note.heading %> <br>
                <strong>Points:</strong> <%= note.points.join(', ') %> <br>
                <strong>Explanation:</strong> <%= note.explain.join(', ') %> 
            </li>
        <% }); %>
    </ul>
</body>
</html>
